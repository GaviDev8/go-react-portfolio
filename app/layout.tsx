import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "./provider";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Gabriela's Portfolio",
  description: "In my portfolio, you'll discover a fusion of strategic insight and technical expertise. With a background in executive business partner experience, project management, and dabbling in development, I specialize in crafting streamlined processes that empower both customers and businesses alike. Explore how I blend creativity with functionality to deliver impactful solutions.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
